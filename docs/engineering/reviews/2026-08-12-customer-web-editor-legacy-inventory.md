# Customer Web editor legacy dependency inventory

Generated by `npm run inventory:editor-legacy` from `apps/customer-web`.

This inventory records the remaining Customer Web editor code that still references the legacy `ScoreEntity` / `Blank` / Verovio space-hit model while REC-007 migrates the editor toward the new `editor-domain` model.

Do not treat this document as approval to keep the legacy model long-term. It is an executable migration ledger: update it when a replacement step intentionally removes or moves a legacy dependency.

## Summary

- Total matching lines: 2
- Files with matches: 2

| Pattern | Matching lines | Meaning |
| --- | ---: | --- |
| Legacy ScoreEntity model | 0 | Old editor entity union used by parser, Inspector, selection, and mutation flows. |
| Legacy Blank type | 0 | Old domain representation for MusicXML <forward>; target model uses timeline gaps instead. |
| Legacy blank entity discriminator | 1 | Old runtime discriminator and comments around blank/forward behavior. |
| Verovio space handling | 1 | Verovio render-space handling must stay adapter-only and must not reintroduce ordinary editable blank event hits. |

## Matches by pattern

### Legacy ScoreEntity model

Old editor entity union used by parser, Inspector, selection, and mutation flows.

No matches.

### Legacy Blank type

Old domain representation for MusicXML <forward>; target model uses timeline gaps instead.

No matches.

### Legacy blank entity discriminator

Old runtime discriminator and comments around blank/forward behavior.

#### `apps/customer-web/src/hooks/editor/use-metadata-editor.ts`

- L96: `// Remove the whole credit element when the text is blank.`

### Verovio space handling

Verovio render-space handling must stay adapter-only and must not reintroduce ordinary editable blank event hits.

#### `apps/customer-web/src/lib/editor/verovio-entity-map.ts`

- L24: `const VEROVIO_SPACE_SELECTOR = '[data-class="space"]';`
