import { InviteShell } from '@/components/shell/invite-shell';

export default function InviteGroupLayout({ children }: { children: React.ReactNode }) {
  return <InviteShell>{children}</InviteShell>;
}
