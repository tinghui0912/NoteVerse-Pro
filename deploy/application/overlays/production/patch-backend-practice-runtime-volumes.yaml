apiVersion: apps/v1
kind: Deployment
metadata:
  name: noteverse-backend-practice
spec:
  template:
    spec:
      containers:
        - name: practice
          volumeMounts:
            - name: work
              mountPath: /var/lib/noteverse/work
      volumes:
        - name: work
          emptyDir: {}
