apiVersion: apps/v1
kind: Deployment
metadata:
  name: noteverse-backend-beat
spec:
  template:
    spec:
      containers:
        - name: beat
          volumeMounts:
            - name: work
              mountPath: /var/lib/noteverse/work
      volumes:
        - name: work
          emptyDir: {}
