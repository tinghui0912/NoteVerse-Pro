# GitOps Release Metadata

This directory is reserved for release metadata snapshots after workflows start
opening GitOps pull requests.

Release metadata should include the full component image set and digests, even
when only one component changed.
