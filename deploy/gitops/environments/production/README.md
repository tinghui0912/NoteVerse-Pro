# Production GitOps Environment

This directory is reserved for the future production Argo CD sync target.

Production desired state must be digest-pinned, reviewed, and free of raw Secret
values. Do not copy minikube-only values or staging-only hosts into this
directory.
