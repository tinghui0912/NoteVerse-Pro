# Web/API runtime
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.28
pydantic[email]==2.13.4
pydantic-settings==2.14.1

# Authentication and security
PyJWT[crypto]==2.12.1
pwdlib[argon2]==0.3.0

# Database, migrations, and storage
sqlmodel==0.0.38
alembic==1.18.4
asyncpg==0.31.0
psycopg[binary]==3.3.4
greenlet==3.5.0
boto3==1.43.29

# Worker and observability
celery==5.6.3
redis==7.4.0
loguru==0.7.3

# Score processing and rendering
numpy==1.26.4
Pillow==12.2.0
verovio==6.2.1
pianoplayer==3.0.2
openpyxl==3.1.5

# OMR/OCR and practice alignment engines
pymatchmaker @ git+https://github.com/pymatchmaker/matchmaker.git@0d106d07d96f9def77de116b29690c262b51b9ee
paddleocr==3.7.0

# GPU framework notes
# - PyTorch/CUDA is provided by noteverse-ml-base, not this file.
# - Docker installs PaddlePaddle separately before installing PaddleOCR so the
#   selected CPU/GPU wheel can be controlled by build args.
